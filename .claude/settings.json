{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"c:/Users/Jespar/Downloads/ux-designer-portfolio-main/ux-designer-portfolio-main\" && git add -A && (git diff --cached --quiet || git commit -m \"Auto-save\") && git push origin main 2>/dev/null || true",
            "async": true,
            "statusMessage": "Pushing to GitHub..."
          }
        ]
      }
    ]
  }
}
