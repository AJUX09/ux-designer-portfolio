{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"e:/Test\" && cp public/css/style.css app/style.css && cp public/css/style.css css/style.css && git add -A && (git diff --cached --quiet || git commit -m \"Auto-save\") && git push 2>/dev/null || true",
            "async": true,
            "statusMessage": "Pushing to GitHub..."
          }
        ]
      }
    ]
  }
}
